{% extends 'base.html.twig' %}

{% block title %}My Tasks{% endblock %}

{% block body %}

<h1>My Tasks</h1>

<a href="{{ path('app_task_new') }}">Create Task</a>

<table>
    <thead>
        <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Status</th>
            <th>Created at</th>
            <th>Updated at</th>
            <th>Actions</th>
        </tr>
    </thead>

    <tbody>
    {% for task in tasks %}
        <tr>
            <td>{{ task.title }}</td>
            <td>{{ task.description }}</td>
            <td>
                {% if task.isDone %}
                    Done
                {% else %}
                    Open
                {% endif %}
            </td>
            <td>{{ task.createdAt|date('Y-m-d H:i') }}</td>
            <td>
                {% if task.updatedAt %}
                    {{ task.updatedAt|date('Y-m-d H:i') }}
                {% else %}
                    -
                {% endif %}
            </td>
            <td>
                <a href="{{ path('app_task_edit', {id: task.id}) }}">Edit</a>
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="3">No tasks yet</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% endblock %}
